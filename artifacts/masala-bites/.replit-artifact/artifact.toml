kind = "web"
previewPath = "/"
title = "Masala Bites"
version = "1.0.0"
id = "artifacts/masala-bites"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24813

[services.development]
run = "pnpm --filter @workspace/masala-bites run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/masala-bites", "run", "build" ]
publicDir = "artifacts/masala-bites/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24813"
BASE_PATH = "/"
